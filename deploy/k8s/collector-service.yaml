apiVersion: v1
kind: Service
metadata:
  name: collector
  namespace: tracing-system
spec:
  selector:
    app: collector
  ports:
    - name: http
      port: 4318
      targetPort: http
    - name: grpc
      port: 4317
      targetPort: grpc
