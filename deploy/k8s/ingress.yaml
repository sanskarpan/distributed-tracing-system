apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tracing-system
  namespace: tracing-system
spec:
  rules:
    - host: tracing.example.internal
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web
                port:
                  number: 80
